/**
	* Data una stringa, restituire una stringa con i caratteri in ordine inverso.
	* Esempio:
	* reverse("ciao") => "oaic"
	*/
/**
 * 
 * @param {Stringa} stringa 
 * @returns Stringa
 */
function reverse(stringa) {
	let inversione = stringa.reverseArraY();
	return inversione;
}